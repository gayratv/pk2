/*/// <reference types="./types/index.d.ts" />*/

import  createDebug from 'debug';

export const debug = createDebug('node-kraken-ws')

// Чтобы увидеть вывод под windows надо дать команду:
// set DEBUG=*
debug('start');
